#include <iostream>
using namespace std;

int main() {
    int first, second;
    cin >> first >> second;
    if (first>second) cout << 1;
    if (first<second) cout << 2;
    if (first==second) cout << 0;

    return 0;
}

// E.